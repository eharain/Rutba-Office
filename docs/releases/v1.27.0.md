# Rutba Office 1.27.0 beta — in the works

In Rutba Word a table of contents that refreshes, tracked changes recorded as
you type, and captions numbered by a field; in Worksheets an outline with
subtotals, an advanced filter, Flash Fill and a watch window; in
Presentation, shapes aligned, grouped, turned and flipped, fills and effects
written, and find and replace across the deck; and in Mail a signature per
account, send later, and automatic replies. Not yet released: this file
gathers what has landed on the branch since 1.26.0.

## Rutba Word

- **Insert → Caption.** A dialog offers Figure, Table or Equation and a
  line of text, previews "Figure 3: …" before OK is pressed, and inserts a
  new paragraph after the caret's — styled "Caption" (written once, italic,
  9pt, space after, the way Word writes one on a document that never had
  the style), the label, a SEQ field for its running number, then the
  words. The number is a COMPLEX field — `w:fldChar` begin/separate/end
  around ` SEQ Figure \* ARABIC `, the shape Word itself writes for a
  caption, unlike a cross-reference's simpler `w:fldSimple` — shaded on the
  page exactly as a REF field is. Update Fields (F9, on the References tab)
  now renumbers every label's captions from where they sit in the document,
  the same pass that refreshes a REF: a caption inserted ahead of others
  with the same label, or one deleted, leaves the rest right once Update
  Fields runs. The button was greyed out ("A caption is a SEQ field; fields
  are not written yet").

- **References → Table of Contents, a field that refreshes.** Until now the
  button typed the headings in as text, which went stale the moment a
  heading changed. It now writes the table Word writes: a building block
  holding a TOC field, one paragraph per heading at three levels, each
  entry a link to a bookmark placed on its heading, a dotted leader and
  the page number the heading lands on in this window's own page layout.
  Update Table, beside it, rebuilds the entries from the headings as they
  now stand (and F9 refreshes it with the other fields); Ctrl+click on an
  entry goes to its heading; Remove Table of Contents takes it away. A
  table written by Word itself is read as the same field and refreshes the
  same way.

- **Review → Track Changes records.** Turning it on is kept in the file, so
  a document opens recording if it was saved that way. While it is on,
  words typed are kept as an insertion and words deleted as a deletion,
  each with its author and time, rather than simply appearing or going;
  deleting words you typed yourself since turning it on takes them straight
  back, as Word does. The Review tab shows it four ways: All Markup
  (insertions underlined and deletions struck through in the author's
  colour), Simple Markup (the finished words with a bar in the margin
  beside each changed paragraph — the default, as in Word), No Markup and
  Original. Accept and Reject act on the change at the caret or on every
  change; Previous and Next walk them. What is not recorded yet: a
  paragraph joined or split (Enter and Backspace across a paragraph's end
  happen for real), a paste, find and replace, and a sort. The buttons
  were greyed out ("recording them is not built yet").

## Worksheets

- **Formulas → Watch Window.** The button now opens a pane docked along the
  bottom of the grid — a table, one row per watched cell: Sheet, Cell,
  Value and Formula. Add Watch adds every cell of the current selection,
  not only the active one; a row's own × takes it off, and Delete Watch
  removes whichever row is selected; a double-click on a row goes to that
  cell, switching sheet first when it is on another one. Every row is
  live: read straight off the calculation on each frame, the same way
  Error Checking rereads its list, so editing a precedent anywhere in the
  workbook moves the watched value at once, with nothing telling the pane
  to refresh. The list is kept for as long as the workbook stays open —
  closing the pane or switching sheets never loses it — but it is never
  written to the file: Excel does not keep its own watch window in the
  workbook either, and this one follows suit. The button was greyed out.

- **Data → Group, Ungroup and the outline.** Rows or columns grouped
  (Shift+Alt+Right) get the gutter Excel draws: a bracket in their level's
  lane, a − box at the summary row that folds them away and turns to +,
  and level buttons 1, 2, 3 in the corner. Groups nest seven deep; Show
  Detail, Hide Detail, Ungroup (Shift+Alt+Left) and Clear Outline act as
  Excel's do, and a selection that is neither whole rows nor whole columns
  is asked about. The file keeps what Excel keeps — each row's and
  column's level, hidden and collapsed marks, the sheet's depth — so an
  outline saved by Excel opens folded or open as it was left. Frozen panes
  and the gutter live together.

- **Data → Subtotal.** At each change in a column, a row of SUBTOTAL
  formulas under every group for the columns ticked, a bold "East Total"
  label, a Grand Total and a three-level outline round them; all eleven of
  Excel's functions, page breaks between groups, summaries above or below,
  and Remove All to take it away. A SUBTOTAL now leaves out the SUBTOTAL
  cells in its own range, as Excel's does, so a Grand Total counts each row
  once. On a 60,000-row list Subtotal takes a few seconds and a frame still
  draws in about 6 ms.

- **Data → Advanced, and Clear.** A list filtered by a criteria range read
  the way Excel reads one: headings name the fields, conditions on a row
  all hold, any one row is enough; `>`, `<=`, `<>` compare, `*`, `?` and `~`
  are wildcards, and a formula under a heading of its own is tried on every
  row. In place, rows that fail are hidden and Clear brings them back;
  copied, the rows that pass go elsewhere under the headings, with Unique
  records only dropping repeats. The names Excel keeps for the list, the
  criteria and the extract are written too.

- **Data → Flash Fill (Ctrl+E).** Type what you want once or twice beside a
  block — "Ada" next to "Lovelace, Ada", or "AL" for initials — and the
  rest of the column is filled. It finds one transformation that makes
  every example: a part cut at a delimiter, a word, digits, the first or
  last characters, initials, a change of case, part of a date, or pieces of
  several columns joined with words of your own. When nothing fits every
  example it says so and fills nothing.

## Presentation

- **Home → Find (Ctrl+F) and Replace (Ctrl+H).** The ribbon's Find and
  Replace buttons now open a small pane at the stage's own top right, the
  way Word's find pane sits over the page rather than a dialog covering
  the slide. A word typed into it is found across every shape's paragraphs
  on every slide — text boxes, placeholders and table cells — read as "n
  of m", Next and Previous walking the hits in slide order and landing on
  the hit's own slide and shape each time; Match case narrows the search.
  Replace rewrites the current hit, Replace All every hit at once, each
  keeping its run's own look — bold, italic, colour, font, size — since
  only the words inside the run change. A match that would straddle two
  runs is not offered: the boundary between two runs is where their look
  can change, and rewriting across it would either lose the second run's
  formatting or have to invent a blend of the two, so only a match sitting
  inside a single run is one Replace can rewrite cleanly. The button was
  greyed out (`docs/GAPS.md`: "find and replace across a deck").

- **Home → Arrange: align, distribute, rotate, flip, group.** Shift+click
  or Ctrl+click gathers shapes into one selection, drawn as one frame;
  dragging any of them moves them all, and Delete and the arrow keys act on
  the lot. Align puts one shape against the slide's edge or centre, and
  several against the edge of the group they make (or of the slide, with
  Align to Slide ticked); Distribute spaces three or more evenly. Rotate
  Right and Left turn by a quarter, Flip Vertical and Horizontal mirror, a
  handle above a single shape turns it by hand (Shift for 15° steps), and
  Alt+Left and Alt+Right turn it by 15° from the keyboard. Group makes one
  shape of several, which moves, resizes, copies and deletes as one and
  sits as one row in the Selection pane; Ungroup puts every member back
  exactly where it now shows, however the group was moved, stretched,
  turned or flipped in between. A group that came in a file now draws its
  members stretched and flipped with it — before, they were only moved.

- **Shape Fill and Shape Effects.** The Format pane's fill is No fill,
  Solid (now with transparency), Gradient (a light or a dark variation of a
  colour, at any angle) or Picture (a picture from a file, filling the
  shape's outline, stretched or tiled). Shape Effects keeps its shadows and
  gains Glow (5, 8, 11 and 18 pt, PowerPoint's own sizes), Soft Edges and
  Reflection (tight, half, full), in any combination, drawn on the slide,
  in the show and in print, and written the way PowerPoint writes them.

## Mail

- **A signature per account.** Set from that account's own settings —
  Folder → Signature — a signature is plain text, kept on the account
  alongside its name and its servers rather than in a preference every
  account shares. A new message carries it at the end, after a blank
  line and the "-- " line mail readers use to tell a signature from the
  words above it; a reply or forward puts the same block above the
  quoted or forwarded message instead, below the cursor rather than
  under it. Switching the From account in an open message swaps the
  block for the one that account keeps — but only when it is still
  exactly what was inserted; a signature already edited, or a body with
  nothing inserted to begin with, is left alone. An account with no
  signature has nothing added.

- **Send later, and an Outbox that is a real place.** Send in a new
  message now offers Send later: later today, tomorrow morning at 8, Monday
  morning at 8, or a date and time of your own, all in the computer's own
  time zone. The message waits in an Outbox kept on this computer, listed
  in the folder list with each message's send time; opening one offers
  Edit (back into the message window), Send now, or Cancel, which puts it
  in Drafts rather than throwing it away. It is sent when its time comes
  while Rutba Office is running, and anything that fell due while it was
  closed goes the moment it opens again; the window says so. A send that
  fails stays in the Outbox with the server's answer beside it and is tried
  again every five minutes, or at once with Retry, where before it was
  moved to Drafts after three tries without a word.

- **Automatic replies, per account.** Folder → Out of office turns them on
  for the account, with an optional start and end, a subject ("Automatic
  reply: " and the original subject unless you write your own), the
  message, and "only reply to people in my contacts". A banner across the
  window says which account is answering on its own, with Turn off beside
  it. A reply keeps the rules that stop two away messages mailing each
  other forever (RFC 3834): nothing already automatic, no bulk or list
  mail, no no-reply or mailer-daemon address and never the account's own
  address gets one, and each sender hears from it once while it is on. The
  reply says what it is (`Auto-Submitted: auto-replied`) and threads under
  the message it answers. Replies go out while Rutba Office is running:
  holding them on the server (Gmail's vacation responder, Microsoft 365's
  automatic replies) needs a sign-in permission this version does not ask
  for, and the dialog says so rather than promising more.

## Proven, not promised

- Nine engine tests in `tests/word-captions.test.js`: a caption lands as
  its own paragraph, styled Caption, right after the caret's; three
  captions of the same label number themselves 1, 2, 3 in document order;
  two labels (Figure, Table) count independently; deleting the middle of
  three captions and calling Update Fields renumbers the one after it;
  the SEQ field's XML matches Word's own form byte-for-byte, and reads
  back through the same field-parsing path a REF uses, shaded the same
  way; the Caption style is written once however many captions follow;
  a caption survives a save and reopen with its field kept, not frozen
  as inert text — deleting one in the reopened copy and updating fields
  still renumbers what is left; a caption paragraph built by hand, the
  way one from Word itself would read, is read as a field and renumbered
  correctly alongside one this engine inserted; and undo after inserting
  a caption takes the whole paragraph back off. The new `captions` window
  check (`RUTBA_VERIFY_ONLY=captions`) places the caret in the fixture,
  inserts a Figure caption from the ribbon dialog, reads "Figure 1: …" on
  the page with its number shaded like any field, inserts a second and
  reads "Figure 2: …", then saves and reads the SEQ field's complex-field
  shape out of the file.

- Twelve engine tests in `tests/word-toc.test.js`: the field and its
  building block written as Word writes them; a bookmark on each heading
  and the three levels kept apart; Update Table after a heading is added,
  removed or renamed; the page numbers the window hands over used, and the
  last ones kept when none are given; the table surviving a save and still
  refreshing after it; a table built the way Word builds one read as a
  field and refreshed; and undo taking it off. The `toc` window block
  (`RUTBA_VERIFY_ONLY=toc`) inserts a table from the ribbon, reads its
  entries on the page with page numbers that differ where the headings
  fall on different pages, adds a heading and presses Update Table, reads
  the field out of the saved file, and finds the window reporting nothing.
  One real fault turned up there and is fixed: the field reader pairs a
  field's start with the nearest end in its paragraph, so a table whose
  opening sat in the same paragraph as its first entry's page number broke
  that entry's link. The table's opening and closing now have paragraphs
  of their own, which shows as a blank line above and below it.

- Thirteen engine tests in `tests/word-track.test.js`: the setting written
  and read back; a typed word recorded as an insertion with its author and
  time; a deleted word kept as a deletion; your own open insertion taken
  back rather than struck; Accept and Reject on one change and on all;
  Previous and Next in document order; a tracked paragraph still editable;
  changes surviving a save; insertions and deletions written by Word read,
  shown and accepted; and undo of a tracked edit. `tests/doc-tracked.test.js`
  was changed on purpose: it held that a paragraph with tracked changes
  could not be edited, which this replaces. The `track` window block
  (`RUTBA_VERIFY_ONLY=track`) turns recording on, types and reads the
  underlined insertion on the page, deletes a word and reads it struck
  through, accepts everything, reads the saved file, turns recording off,
  and finds the window reporting nothing.

- Six engine tests in `tests/sheet-watch.test.js`: adding a cell already
  on the list, or twice in one call, leaves it once; new refs land after
  what was already there, in the order given; removing one watch leaves
  the rest in place, in order, and a ref nobody is watching changes
  nothing; a resolved row carries its sheet, reference, live value and
  formula text (`null` for a plain value); a precedent edited after a
  watch is added moves its value on the next read, with nothing telling
  it to refresh; and a watch on a sheet since removed from the workbook
  drops out of the resolved list rather than throwing. Five window checks
  in the new `watch` block (`RUTBA_VERIFY_ONLY=watch`): select a formula
  cell already in the sample workbook, open the pane, Add Watch, and read
  its row's sheet, value and formula; edit the precedent the formula
  reads through the ordinary engine ops and watch the row follow it live;
  take the watch off with the row's own × and see it go; and Close turns
  the pane off, the button lifting with it, and the window reports
  nothing throughout.

- Twenty-eight engine tests across `tests/sheet-outline.test.js` (8),
  `tests/sheet-subtotals.test.js` (6), `tests/sheet-advanced-filter.test.js`
  (6) and `tests/sheet-flash-fill.test.js` (8): levels written and taken
  off, nesting and the eighth level refused, folds writing hidden and
  collapsed, columns grouped, an outline in Excel's own XML read as groups;
  subtotal rows, formulas, labels and outline, nested SUBTOTALs left out,
  Replace against nesting, Remove All back to the list in one undo step;
  Excel's reading of each kind of criteria cell, OR'ed rows, AND'ed
  columns, unique records, formula criteria, the names written; and Flash
  Fill's first names, initials, case, delimiters, digits, joined columns,
  dates, a second example settling an ambiguity and no pattern meaning no
  fill. Two new window blocks: `outline` (`RUTBA_VERIFY_ONLY=outline`)
  groups rows under a frozen top row and measures the gutter against every
  heading to the pixel, folds by box and by level, groups columns,
  subtotals a list and reads 550, 400, 420 and 1370 off the grid, then
  removes it all and finds the saved file as it was; `datatools`
  (`RUTBA_VERIFY_ONLY=datatools`) filters in place by two OR'ed rows,
  clears, copies unique rows elsewhere, fills first names with Ctrl+E and
  initials from the ribbon, and undoes every step.

- Nine engine tests in `tests/deck-find.test.js`: two cover the deck's
  existing shape-by-shape `findText`/`replaceText`; seven new ones cover
  `find`, `replace` and `replaceAll` — hits across two slides in slide,
  shape, paragraph and run order; case sensitivity on and off; no hits for
  a word the deck does not hold; a single replace keeping its run's bold,
  italic and size while the rest of the words are untouched; Replace All's
  count and the rewritten words surviving three hits inside one run
  without one replacement's length throwing off the offsets of the next;
  a hit inside a table cell, carrying its row and column, found and
  replaced there too; and find, replace and replaceAll all surviving a
  save. Seven window checks in the `deckfind` block
  (`RUTBA_VERIFY_ONLY=deckfind`, aliased from the existing `find` block):
  the pane opens from Ctrl+H or the ribbon's Replace button and reads "1
  of 2" on typing a word, landing on its first hit's slide; Next moves to
  the next hit's slide, the count following it; Match case narrows the
  search to nothing for a query only case tells apart; Replace All rewrites
  both hits and reports the count; the saved file carries the replaced
  words; the deck is left exactly as the check found it; and the window
  reports nothing.

- Twelve engine tests in `tests/deck-arrange.test.js`: every edge aligned
  to the slide and to a selection, one shape always to the slide; a turned
  shape aligned by what it covers; even spacing across a selection and
  across the slide; rotation wrapping past 360° with each shape turning on
  its own centre; a flip surviving a save with its rotation untouched; a
  new group's own frame matching what it holds; a group inside a group; a
  moved and stretched group carrying its members; and group then ungroup
  leaving every member where it showed, through a move, a stretch, a turn
  and a flip together. The `arrange` window block
  (`RUTBA_VERIFY_ONLY=arrange`) adds two shapes, Shift+clicks the second
  into the selection, aligns them, groups them, moves the group from the
  keyboard, ungroups and finds the members where the group left them,
  turns and flips one and reads both back out of the saved file, and
  leaves the deck as it found it. It caught one fault, now fixed: one
  shape aligned to the other shapes' group whenever the ribbon said so
  outright, which it always does.

- Twelve engine tests in `tests/deck-fill-effects.test.js`: a solid fill's
  transparency; a gradient's every stop and its angle read back; the light
  and dark variations; a picture fill's picture and its link to the slide
  surviving a save; tiling, and changing it without sending the picture
  again; a replaced picture leaving the old one unreferenced; glow, soft
  edges and each reflection written and read; all four effects together in
  the order the file format sets, and None clearing them; one more effect
  added without losing those already on. The `deckfx` window block
  (`RUTBA_VERIFY_ONLY=deckfx`) sets a gradient from the Format pane and
  turns it, finds the gradient drawn on the slide, switches to a
  see-through solid fill, adds glow, soft edges and reflection from the
  ribbon, checking each keeps the ones before it, finds the glow drawn,
  reads fill and effects out of the saved file, and leaves the deck as it
  was. It caught a fault no engine test could: adding a second effect
  reset the first to a default size, because the pane handed back sizes
  in pixels where the writer takes points.

- Seven engine tests in `tests/mail-signature.test.js`: a new message
  gets the signature at the end after a blank line and the "-- " line, a
  reply puts it above the quote and below the cursor, an empty
  signature is a no-op for a new message and a reply alike, switching
  accounts swaps the block — at the end of a new message and above a
  reply's quote — when it is still exactly what was inserted, an edited
  signature is left alone on the next switch, and there is nothing to
  swap when the old account carried no signature. One window check in
  the new `signature` block (`RUTBA_VERIFY_ONLY=signature`) sets a
  signature from the seeded account's own Folder → Signature dialog,
  opens a new message and reads its plain-text body for the blank
  line, the "-- " line and the signature, then opens a reply and finds
  the same block above the quote, and the window reports nothing.

- Eight engine tests in `tests/mail-sendlater.test.js` and eight in
  `tests/mail-ooo.test.js`. Send later: the choices fall where they say,
  Monday morning included across a weekend; the Outbox sends only what is
  due, oldest first, and leaves the rest; a failed send stays with its
  error and goes again on the next pass; Cancel takes it off the queue
  unsent; the queue survives a restart, and what fell due while closed
  goes at start-up. Automatic replies: each of the RFC 3834 skips, one
  reply per sender while it is on, the start and end honoured to the
  minute on either side, contacts-only, the reply's headers, and turning
  it on again starting the list of who has been answered afresh. The mail
  service now takes its clock and its sending transport as arguments,
  which is what lets all sixteen run with no timer and no network. Two new
  window blocks: `sendlater` (`RUTBA_VERIFY_ONLY=sendlater`) sends a
  message later from the message window, reads it in the Outbox with its
  time, presses Send now and finds it gone from the Outbox and in Sent;
  `ooo` (`RUTBA_VERIFY_ONLY=ooo`) turns automatic replies on from the
  account's dialog, reads the banner, delivers a message through a
  stand-in for new mail arriving (it refuses to run outside a check) and
  finds exactly one reply sent, with its headers, then a second message
  from the same sender and no second reply; both blocks read that the
  window reported nothing.

- The whole gate is open on Windows on the branch as it stands (2026-09-25):
  987 engine tests, the 12 editing checks, 498 application checks and every
  window painting in the smoke run. The first four blocks here — `captions`,
  `watch`, `deckfind` and `signature` — had passed under Xvfb in a Linux
  container before this. The Windows gate found one fault of the product's
  own, now fixed: Replace All's "Replaced 2 across the deck." gave way at
  once to the search that followed it, which, finding nothing left, said
  "No matches.". The rest were in the checks themselves, and are fixed
  there. The editing pass waits for each key to reach the document rather
  than sleeping a fixed time. A full run leaves its windows for Electron to
  close on the way out; destroying ninety of them just before made the run
  end on exit code 7014 whatever its verdict. Checks written before this
  version now expect the table of contents as a field and a shadow drawn by
  offset and blur.
