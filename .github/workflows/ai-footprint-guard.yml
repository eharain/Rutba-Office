name: AI Footprint Guard

on:
  pull_request:
    types: [opened, synchronize, reopened, edited, ready_for_review]

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Check for AI footprints in commits and changed files
        run: |
          git fetch --no-tags --prune origin "${{ github.event.pull_request.base.ref }}"
          BASE_SHA="$(git merge-base 'origin/${{ github.event.pull_request.base.ref }}' '${{ github.event.pull_request.head.sha }}')"
          HEAD_SHA="${{ github.event.pull_request.head.sha }}"
          SCRIPT_PATH="/tmp/ai-footprint-guard.sh"
          BASE_REF="origin/${{ github.event.pull_request.base.ref }}"
          if git cat-file -e "$BASE_REF:tools/ci/ai-footprint-guard.sh" 2>/dev/null; then
            git show "$BASE_REF:tools/ci/ai-footprint-guard.sh" > "$SCRIPT_PATH"
          else
            echo "Trusted guard script not found on base branch; refusing to skip enforcement." >&2
            exit 1
          fi
          chmod +x "$SCRIPT_PATH"
          bash "$SCRIPT_PATH" "$BASE_SHA" "$HEAD_SHA"
